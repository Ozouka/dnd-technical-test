[deploy]
startCommand = "sh -c 'npx prisma migrate deploy && npx react-router-serve ./build/server/index.js'"
healthcheckPath = "/"
healthcheckTimeout = 300

[deploy.environmentVariables]
HOST = "0.0.0.0"
